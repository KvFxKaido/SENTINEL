# Normalize all text files to LF, regardless of checkout platform.
* text=auto eol=lf

# Windows batch files require CRLF to execute correctly.
*.cmd text eol=crlf
*.bat text eol=crlf

# Image assets must never be EOL-normalized.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
