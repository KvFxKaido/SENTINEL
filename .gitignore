# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
ENV/
env/
.venv/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/
sentinel-agent/.pytest_cache/
sentinel-agent/tmp*/

# Project specific
sentinel-agent/campaigns/
campaigns/
wiki/campaigns/*/
wiki/.obsidian/
simulations/
*.json.bak
# assets/portraits/ and assets/characters/ are intentionally TRACKED —
# Tier 1 source IP. Un-ignored during the video-game restructure so archiving
# the repo can never lose them. Portraits committed directly (static, ~20MB,
# archive-safe) rather than via LFS to avoid an external-store dependency.
# See architecture/VIDEO_GAME_REUSE_MAP.md.
# Negation re-includes the campaign-isolated specs that the broad `campaigns/`
# rule (above) would otherwise re-hide.
!assets/characters/campaigns/
nanobanana-output/
sentinel-agent/generated_npcs/
sentinel-agent/test_portrait.py

# Personal
personal/

# WIP / scratch files
WARP.md

# Personal workspace tools
Vigil/
workspace.cmd
workspace.ps1
.claude/personal.md

# Environment
.env
.env.local
*.local.json
.mcp.json

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Windows reserved names (can't be tracked)
nul
tmpclaude-*

.gemini/
gha-creds-*.json

# Cloudflare Workers local state
.wrangler/
# Godot 4 import cache
.godot/
# assets/packs/ — every third-party pack, licensed or reference, QUARANTINED
# under one root so one rule covers them all. Licensed packs are NEVER
# committed: the licenses permit use in a project but not redistribution as
# an asset, and this repo is public. Reference packs (Character Creator 2D)
# ride the same rule — pose grammar, not architecture, and not ours.
assets/packs/
# Safety net, unanchored on purpose: a pack unpacked anywhere OUTSIDE the
# quarantine is still caught by name shape before the next `git add -A` —
# which is exactly how NEW-TopDownCharacter-v1.3 reached master in 4c384a9.
# The FREE Character glob is load-bearing for OTHER checkouts: this commit
# moved those packs on one machine, but a checkout that still holds them in
# prototypes/ would see them go untracked the moment the old ignore lines
# vanished — the 4c384a9 failure, reintroduced by the fix (caught in review).
*TopDownCharacter*/
*Pixel Art*/
FREE Character */
# regenerable sprite outputs — the whole roster (scripts/roster_mold.py)
# plus the System A walk-off strips (scripts/cipher32_walk.py); the
# committed scripts are the decision records, the pixels rebuild.
# assets/original/ is deliberately ABSENT here — tracked source art
# (original_body_pipeline.md, D9). Precisely: the .aseprite hand-work and
# curated PixelLab rolls do NOT rebuild (rolls are stochastic); the
# cipher_render sheets technically re-download via walkoff_render_sheets.py
# for as long as PixelLab's CDN holds them, and a CDN is not provenance.
assets/sprites/

# walkable harness deps (prototypes/walkable/test)
node_modules/

# Audition captures — screenshots a human judges, regenerated by
# prototypes/walkable/test/audition_generated.mjs on demand.
prototypes/walkable/test/audition-*/
